// Simple middleware that does nothing - no authentication required
export function middleware() {
  // No middleware processing needed for simple chess app
  return
}

export const config = {
  matcher: [],
}
